
server:
  port: 8085
  
spring:
  application:
    name: DocumentProcessingApplication
  datasource:
    url: jdbc:mysql://localhost:3306/camunda
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

